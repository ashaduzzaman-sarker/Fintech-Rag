# Streamlit secrets configuration
# Copy this to .streamlit/secrets.toml and fill in your values

# API Configuration
API_BASE_URL = "http://localhost:8000"

# Optional: Add authentication tokens
# API_TOKEN = "your-api-token"
